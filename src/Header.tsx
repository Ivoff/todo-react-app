import { ReactElement } from "react";

export default function Header(): ReactElement {
    return (
        <div className="py-6 bg-violet-200">
            <h1 className="text-4xl text-violet-500 text-center font-mono font-bold">//TODO: App</h1>            
        </div>
    ); 
}